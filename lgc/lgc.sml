(* Copyright 2019 Systems & Technology Research. All Rights Reserved. *)
structure Lgc :> LGC =
struct

(* See lgc.sig for the expected types of these functions. *)

(* Parallel PR-Nibble algorithm *)
fun parallelPrNibble (seeds, graph, alpha, epsilon) : real array list =
    raise Fail "Not implemented"

(* ISTA algorithm *)
fun ista (seeds, graph, alpha, rho, iters) : real array list =
    raise Fail "Not implemented"

end
